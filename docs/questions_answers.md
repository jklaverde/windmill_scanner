Q46 — Farm-level ETL button in-flight state

The per-windmill ETL button is documented: disabled + spinner while in-flight. The farm-level ETL button (middle panel header) has no equivalent. When the farm ETL  
 request is in-flight, what does that button do?

- (a) Disabled + spinner (same behaviour as the per-windmill button)
- (b) Fire-and-forget — no disabled state (same behaviour as Start All / Stop All)

A:/ (a)

---

Q47 — Windmill row: description display

Farm rows show the description on hover (tooltip). Windmill rows are documented to show: name + windmill_id + is_running badge + sensor_beat + location_beat. The
windmill description is not mentioned.

- (a) Hovering the windmill row shows the description in a tooltip (same as farms)
- (b) Description is not shown in the list row — only visible in edit mode
- (c) Description shown as a secondary text line within the row (always visible, below the name)

A:/ (a)
